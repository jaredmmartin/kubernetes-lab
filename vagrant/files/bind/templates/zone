$TTL    600
@       IN      SOA     {{ ansible_hostname }}.{{ dns_zone }} {{ dns_zone }}. (
                5       ; Serial
                604800  ; Refresh
                86400   ; Retry
                2419200 ; Expire
                604800 ); Negative Cache TTL
;
; name servers - NS records
    IN      NS      {{ ansible_hostname }}.{{ dns_zone }}.

; name servers
{{ ansible_hostname }}.{{ dns_zone }}.  IN  A {{ dns_server_ip }}